
function App() {
  return (
    <div className="App">
     <h1>hi</h1>
     <p> just testing my github repo if it's linked properly through remote</p>
     <p> and if i need to use git pull origin branchname --allow-unrelated-histories</p>
    </div>
  );
}

export default App;
